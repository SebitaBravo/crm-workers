export const isActiveSession = (token, expirationDate) => {
    if (!token ) {
        return false;
    }

    // const now = new Date();
    // const expiration = new Date(expirationDate);

    return true;
}
